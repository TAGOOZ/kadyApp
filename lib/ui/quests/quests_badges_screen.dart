// Placeholder replaced by slice #010.
import 'package:flutter/material.dart';

class QuestsBadgesScreen extends StatelessWidget {
  const QuestsBadgesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('المهام والشارات')),
      body: const Center(child: Text('قريبًا')),
    );
  }
}
