# Implementation Plans

Generated by the improve skill on 2026-08-23 against commit `0c1e59d`.
Non-interactive run: the default selection (top findings by leverage) was used —
no user was available to pick. Execute in the order below unless dependencies
say otherwise. Each executor: read the plan fully before starting, honor its
STOP conditions, and update your row when done.

## Verification baseline (recorded at `0c1e59d`)

| Gate            | Command           | Expected output                    |
|-----------------|-------------------|------------------------------------|
| Static analysis | `flutter analyze` | `No issues found!`                 |
| Test suite      | `flutter test`    | `+277: All tests passed!` (~41 s)  |

Every plan's done criteria build on these two gates.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Redeemed Points actually deducted at checkout (`applyRedemption` wired through crediting) | P1 | M | — | DONE |
| 002 | Stamp-card math unified behind one pure rule (3 divergent implementations → 1) | P2 | M | none (re-check drift vs 001 if landed after it; both touch `loyalty_rules.dart`) | DONE |
| 003 | Banner carousel reduce-motion honored after touch + data-driven page count | P2 | S | — | TODO |
| 004 | Order-status surfaces follow the language toggle (ar/en routing) | P2 | S | — | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- All four plans are independent; recommended order is severity-ranked (001 first — revenue-impacting bug).
- 001 and 002 both modify `lib/domain/loyalty_rules.dart`. Land sequentially (either order); the second executor must re-run its plan's drift check and expect the "Current state" excerpts of untouched regions to still match.
- None of the plans require SQL/migrations or a live Supabase project; all verification is local (`flutter analyze` / `flutter test`).

## Findings considered and rejected

Recorded so the next audit doesn't re-surface them:

- **Hardcoded Supabase URL + publishable key in `lib/core/supabase/supabase_config.dart:5-6`**: by-design — ADR-0001 explicitly bundles URL/publishable key in-app ("public"), secret stays server-only in `.env`. Verified `.env` is gitignored and no secret values exist in tracked files.
- **Client-side loyalty crediting via RLS own-row UPDATE (cheat vector)**: documented accepted trade-off (ADR-0007 note in FEATURES §4; comments in `loyalty_controller.dart`). Edge Function deferred by decision — surfaced as a *direction* option, not a defect.
- **Driver board showing all drivers' deliveries / `assigned_driver` null stub**: documented in `driver_orders_repository.dart` header ("admin assignment UI lands later").
- **Check-in stamps degrading to `loyaltyPending` under RLS until an Edge Function exists**: documented trade-off (staff/lookup repo comments). Plan 002 only fixes the *math* of the write, not the pending posture.
- **Google-gate auth path, simulated-vs-real choices, no OTP**: locked §11 decisions.
- **No analytics/crash-reporting/push in MVP; manual builds only; single Supabase project; online-only error policy; Western digits; local #NNNN ids**: all locked §11 decisions — excluded from findings.
- **Duplicate top-level `/games` GoRoute shadowed by the shell branch (`lib/core/router.dart:182-185`)**: dead config, zero runtime effect; not worth a plan.
- **Dead getter `ItemConfig.identityKey` (`menu_models.dart:89`, no callers)**: trivial; remove opportunistically during any menu-model work; not worth a dedicated plan.
- **Guest flow docs mismatch** (FEATURES §3.1 says guests get one order; checkout bounces guests pre-order because RLS forbids guest inserts): real doc staleness, but a one-line doc edit; fold into any future docs pass rather than a plan.

## Unplanned vetted findings (candidates for a future round)

- **[perf]** Staff board pulls the ENTIRE `customers` table per mount to decorate names (`fetchCustomers`, `staff_orders_repository.dart:290-298`; provider :558). Replace with an embedded join on orders or targeted fetches once convenient.
- **[perf]** Admin KPI probe selects ALL orders ever with no limit/range (`admin_repositories.dart:607-615`) and aggregates client-side.
- **[perf]** Unbounded reads vs the documented paginate-20 decision (§11.27): `fetchOwnOrders` has no limit (`order_status_repository.dart:117-130`); visit counting downloads every visit id (`customer_lookup_repository.dart:360-368`).
- **[security/hardening]** `orders_status_ops_update` policy lets staff/driver/admin update every column of every order (`0001_init.sql:590-595`); consider a transition-guard trigger + driver scoping when the Edge Function phase starts (needs a live DB to verify — poor fit for local-only execution).
- **[correctness/narrow]** `refreshFor()` zeroes displayed loyalty state on ANY fetch error including transient network (`loyalty_controller.dart:146-148`); later grants would persist from the wiped base. Exposure limited to the profile refresh button today.
