# Implementation Plans

Generated by the improve skill on 2026-08-25 against commit `69a434e`.
Standard audit (recon + 4 parallel subagents, very thorough correctness/security). Non-interactive run: top findings by leverage (impact/effort, confidence, risk) were default-selected — user can re-pick. Execute in order below unless dependencies say otherwise. Each executor: read plan fully before starting, honor STOP, update row when done.

## Verification baseline (recorded at `69a434e`)

| Gate | Command | Expected output |
|------|---------|-----------------|
| Static analysis | `export PATH="$HOME/flutter/bin:$PATH" && flutter analyze` | `No issues found!` |
| Test suite | `export PATH="$HOME/flutter/bin:$PATH" && flutter test` | `+426: All tests passed!` (~70s) |

Every plan's done criteria build on these two gates. All plans assume `flutter pub get` already run.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status | Issue |
|------|-------|----------|--------|------------|--------|-------|
| 001 | Redeemed Points actually deducted at checkout (`applyRedemption` wired through crediting) | P1 | M | — | DONE | — |
| 002 | Stamp-card math unified behind one pure rule (3 divergent implementations → 1) | P2 | M | none (re-check drift vs 001 if landed after it; both touch `loyalty_rules.dart`) | DONE | — |
| 003 | Banner carousel reduce-motion honored after touch + data-driven page count | P2 | S | — | DONE | — |
| 004 | Order-status surfaces follow the language toggle (ar/en routing) | P2 | S | — | DONE | — |
| 005 | Harden avatars storage RLS to owner-only folder | P1 | S | — | DONE | [#17](https://github.com/TAGOOZ/kadyApp/issues/17) |
| 006 | Fix profile avatar pick on web (remove dart:io File) | P1 | S | — | DONE | [#18](https://github.com/TAGOOZ/kadyApp/issues/18) |
| 007 | Validate redemption cost server-side (reject forged notes prefix) | P1 | S | — | DONE | [#19](https://github.com/TAGOOZ/kadyApp/issues/19) |
| 008 | Parallelize staff customer lookup and escape PostgREST filters | P1 | S | — | DONE | [#20](https://github.com/TAGOOZ/kadyApp/issues/20) |
| 009 | Fix menu category crash on empty catalog and driver stepper stale state | P2 | S | — | DONE | [#21](https://github.com/TAGOOZ/kadyApp/issues/21) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 001–004 are DONE and independent (see original notes). Keep sequential land for 001/002 if re-run.
- 005–009 are independent and can run in parallel; recommended order is severity-ranked (005-008 first — security/perf). 005 touches `supabase/migrations/0010_avatars.sql` only via new `0011` migration, so no conflict with 006 which touches `lib/ui/profile/profile_screen.dart`. 007 touches only new `0012` migration. 008 touches `lib/ui/lookup/customer_lookup_screen.dart` + `lib/data/repos/customer_lookup_repository.dart`. 009 touches `lib/ui/menu/menu_screen.dart` + `lib/ui/driver/driver_home_screen.dart`.
- Router was refactored after `69a434e` to fix UX (hydration guard, `_SessionRefresh` selective listeners, `StatefulShellRoute` keys, deferred loader memoization, `/orders/:id` nesting, `_NotFoundScreen`). Plans that touch `lib/core/router.dart` would need drift re-check; none of 005-009 do, so safe.
- Logout wiring was added after `69a434e` (`lib/core/logout.dart`, `lib/domain/auth_controller.dart:145` global scope, logout buttons in staff/driver/admin). No impact on 005-009.

## Findings considered and rejected

Recorded so next audit doesn't re-surface them:

- **Hardcoded Supabase URL + publishable key in `lib/core/supabase/supabase_config.dart:5-6`**: by-design — ADR-0001 explicitly bundles URL/publishable key in-app ("public"), secret stays server-only in `.env`. Verified `.env` is gitignored and no secret values in tracked files.
- **Client-side loyalty crediting via RLS own-row UPDATE (cheat vector)**: documented accepted trade-off (ADR-0007 note in FEATURES §4; comments in `loyalty_controller.dart`). Edge Function deferred by decision — surfaced as *direction* option, not a defect. However **SECURITY-01/02/03** below are narrower server-validation gaps that *are* worth fixing without full RPC migration (see 007).
- **Driver board showing all drivers' deliveries / `assigned_driver` null stub**: documented in `driver_orders_repository.dart` header ("admin assignment UI lands later").
- **Check-in stamps degrading to `loyaltyPending` under RLS until Edge Function exists**: documented trade-off. Plan 002 only fixes math.
- **Google-gate auth path, no OTP, simulated-vs-real choices**: locked §11 decisions.
- **No analytics/crash-reporting/push in MVP; manual builds only; single Supabase project; online-only error policy; Western digits; local #NNNN ids**: all locked §11 decisions — excluded.
- **Duplicate top-level `/games` GoRoute shadowed by shell branch (`lib/core/router.dart:182-185` pre-refactor)**: fixed in router refactor (removed duplicate), no plan needed.
- **Dead getter `ItemConfig.identityKey` (`menu_models.dart:89`, no callers)**: trivial; remove opportunistically; not worth dedicated plan.
- **Guest flow docs mismatch** (FEATURES §3.1 says guests get one order; checkout bounces guests pre-order because RLS forbids guest inserts): one-line doc edit; fold into docs pass.

Newly vetted but not planned this round (candidates for next round):

- **[perf]** Staff board pulls ENTIRE `customers` table per mount to decorate names (`staff_orders_repository.dart:290`); admin KPI probe selects ALL orders ever (`admin_repositories.dart:607`); `fetchOwnOrders` no limit (`order_status_repository.dart:117`); visit counting downloads every id (`customer_lookup_repository.dart:360`). See PERFORMANCE-02/05 in audit — candidates.
- **[security/hardening]** `orders_status_ops_update` policy lets staff/driver/admin update every column (`0001_init.sql:590`); needs transition-guard trigger + driver scoping (needs live DB).
- **[correctness/narrow]** `refreshFor()` zeroes loyalty state on ANY fetch error (`loyalty_controller.dart:146`); later grants would persist from wiped base. Limited to profile refresh button.
- **[arch]** God modules 600-900 lines (`admin_repositories.dart:772`, `staff_orders_repository.dart:726`, `checkout_screen.dart:932`) — split needed but L effort, defer.
- **[arch]** UI hits Supabase directly bypassing seam (`hours_editor_panel.dart:36`, `zones_editor_panel.dart:39`, `driver_home_screen.dart:467`, `profile_screen.dart:90` storage) — violates layer-first, M effort.
- **[test]** Loyalty controller critical credit path untested (`loyalty_controller_test.dart:44` only tier/voucher); checkout pickup/delivery uncovered; profile/quest CRUD zero coverage — see TEST-01..03.

## Direction findings (not ranked against bugs — options for maintainer)

- **Unfinished intent: Quest pending-grants queue**: `lib/data/repos/quest_state_store.dart:58` `claimedQuests`/`pendingGrants` json queue is documented deviation for missing `grantTokens` seam. Evidence: TODO clusters around quests, stubbed `grantStampsPure`. Adjacent possible: expose `grantTokens` RPC and drain queue server-side; Effort M; Trade-off: needs RPC + client resync, but unlocks reliable rewards.
- **Surface asymmetry: Admin menu CRUD has reinsertRow for undo but categories do not**: `lib/data/repos/admin_repositories.dart:534` `reinsertRow(payload)` for items, no category reinsert. Cheap to add symmetry; Effort S.
- **Adjacent possible: Maps bundle is eager for all customers**: `lib/ui/orders/order_status_screen.dart:13` + `driver_home_screen.dart:26` import `flutter_map`/`geolocator` eagerly; only delivery flows use it. Could defer via `deferred import` like heavy games routes (ADR-0011), saving ~150-250KB gzipped initial JS; Effort M.

## Audit coverage

- **Audited**: correctness (8 findings), security (6), performance (7), test coverage + tech debt/architecture (10) via 4 subagents at standard depth (very thorough correctness/security, medium others). Files sampled: `lib/core/router.dart`, `lib/domain/*`, `lib/data/repos/*`, `lib/ui/**/*`, `supabase/migrations/*.sql`, `test/**/*`.
- **Not audited at this depth**: `android/`, `ios/`, `web/` platform configs (per AGENTS.md don't touch), `worker/` Cloudflare Worker (only health/webhook CSP checked in SECURITY-06), `docs/adr/*` beyond reading for vetting, full `npm audit`/`osv-scanner` for dependencies ( pubspec versions noted but not scanned).
- **Vetted**: subagent findings checked against live code; rejected by-design items moved to rejected section (e.g., hardcoded publishable key, proxy honoring, documented pending trade-offs).

## Audit follow-through

- #5–#7 perf bounds: fixed (merge of fix/perf-bounds)
- #3,#4 UI fixes: fixed (fix/ui-small)
- #8: migration `supabase/migrations/0003_order_update_hardening.sql` written (live-DB verification pending owner run)
- #9: refreshFor keeps last-known state on failure
- #10: riverpod 3.x migration DONE (ADR-0012, global no-auto-retry)
- Router UX refactor (hydration guard, selective refresh, deferred memoization, orders nesting, NotFound) — done post-`69a434e` (see git diff)
- Logout wired for all roles (`lib/core/logout.dart`, global SignOutScope) — done post-`69a434e`
